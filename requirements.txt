# Core
streamlit
pandas
numpy
openpyxl

# Scraping
requests
beautifulsoup4
playwright          # after installing, also run: playwright install chromium

# Optional
xlrd                # read legacy .xls files
duckdb              # faster matching on very large datasets
# pyodbc            # only if reading Access .accdb/.mdb files on Windows
